window.NATUREGEN_CONFIG = {
  // Paste these two values from Supabase Dashboard → Project Settings → API.
  // Use ONLY the browser-safe publishable/anon key here. Never use service_role.
  SUPABASE_URL: "PASTE_SUPABASE_PROJECT_URL_HERE",
  SUPABASE_KEY: "PASTE_SUPABASE_PUBLISHABLE_OR_ANON_KEY_HERE"
};
